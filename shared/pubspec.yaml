name: shared
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  android_id:
  async:
  dartx:
  decimal:
  device_info_plus:
  flutter:
    sdk: flutter
  flutter_cache_manager:
  flutter_udid:
  injectable:
  intl:
  mime:
  package_info_plus:
  path_provider:
  rxdart:
  timezone:
  url_launcher:

dev_dependencies:
  build_runner:
  custom_lint:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  freezed_annotation:
  injectable_generator:
  logging:
  mocktail:
  nals_lints:
    path: ../nals_lints